# AGENTS.md

이 문서는 Claude와 Codex가 동일한 방향으로 작업하기 위한 공통 기준이다.

## 프로젝트 원칙

1. `altarry-prog/chaekiji`는 기능 아이디어와 사용자 흐름을 이해하기 위한 참고자료로만 사용한다.
2. 참고 저장소의 코드, 문구, 이미지, 디자인 자산을 복제하지 않는다.
3. 모든 구현은 `mjh20057-dot/bucheon-ai-library`에서 독립적으로 작성한다.
4. 특정 도서관에 종속되는 값은 설정과 커넥터 계층으로 분리한다.

## 기술 기준

- React와 Vite를 기본 프론트엔드 스택으로 사용한다.
- 화면 컴포넌트와 외부 API 서비스 계층을 분리한다.
- API 키, 비밀값, 개인정보는 클라이언트 번들 및 Git 저장소에 포함하지 않는다.
- 정보나루 API는 서버에서만 호출하며 인증키를 클라이언트 코드나 응답에 포함하지 않는다.
- 실제 데이터의 출처와 대출 상태의 조회 시점을 사용자가 확인할 수 있게 표시한다.

## 현재 우선순위

1. 검색 화면과 결과 구조
2. 도서관 정보나루 연계 인터페이스
3. 소장 및 대출상태 표현
4. 한국어 STT
5. 자연어 질의 해석과 백엔드
6. ALPAS·SSO 커넥터

## 품질 기준

- 모바일과 데스크톱에서 사용할 수 있어야 한다.
- 키보드 조작, 대체 텍스트, 명확한 레이블 등 웹 접근성을 고려한다.
- 로딩, 빈 결과, 네트워크 오류, API 제한 상태를 각각 처리한다.
- 새 기능에는 최소한의 검증 방법 또는 테스트를 함께 기록한다.
- README의 실제 구현 현황을 변경사항과 함께 갱신한다.
